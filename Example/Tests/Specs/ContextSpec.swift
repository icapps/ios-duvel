//
//  ContextSpec.swift
//  Duvel
//
//  Created by Jelle Vandebeeck on 08/06/16.
//  Copyright © 2016 CocoaPods. All rights reserved.
//

import Quick
import Nimble
import Duvel

class ContextSpec: QuickSpec {
    override func spec() {
        
        describe("managed object context") {
            pending("should be able to create a main context.") {
                // duvel.mainContext
                // duvel.backgroundContext
                // duvel.currentContext
            }
            
            pending("should be able to create a background context.") {
                // duvel.mainContext
                // duvel.backgroundContext
                // duvel.currentContext
            }
            
            pending("should be able to create a current context.") {
                // duvel.mainContext
                // duvel.backgroundContext
                // duvel.currentContext
            }
        }
        
    }
}
